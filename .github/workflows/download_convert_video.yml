name: Download Video with yt-dlp

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'URL de la vidéo à télécharger'
        required: true

jobs:
  download_video:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install yt-dlp
        run: |
          python -m pip install --upgrade pip
          pip install yt-dlp

      - name: Download video
        run: |
          yt-dlp "${{ github.event.inputs.video_url }}" -o "downloaded_video.mp4"

      - name: Upload downloaded video as artifact
        uses: actions/upload-artifact@v3
        with:
          name: downloaded_video
          path: downloaded_video.mp4
